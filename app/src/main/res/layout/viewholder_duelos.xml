<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_marginTop="10dp"
    android:layout_marginLeft="7dp"
    android:layout_marginRight="7dp"
    android:layout_marginBottom="5dp"
    android:elevation="10dp"
    android:backgroundTint="#FBFCF9"
    android:background="@drawable/styles_edittext"
    android:layout_height="110dp">

    <ImageView
        android:id="@+id/jugador1Duelo"
        android:layout_width="90dp"
        android:layout_height="90dp"
        android:elevation="10dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <ImageView
        android:id="@+id/fotoEstadisticaJ1"
        android:layout_width="25dp"
        android:layout_height="25dp"
        android:elevation="10dp"
        app:layout_constraintTop_toTopOf="@id/tipo"
        app:layout_constraintLeft_toRightOf="@id/jugador1Duelo"
        app:layout_constraintBottom_toBottomOf="@id/tipo"/>

    <TextView
        android:id="@+id/tipo"
        android:layout_width="115dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:elevation="10dp"
        android:textSize="17dp"
        android:textColor="@color/black"
        android:gravity="center"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toRightOf="@id/jugador1Duelo"
        app:layout_constraintRight_toLeftOf="@id/jugador2Duelo"
        app:layout_constraintBottom_toBottomOf="parent"/>


    <ImageView
        android:id="@+id/jugador2Duelo"
        android:layout_width="90dp"
        android:layout_height="90dp"
        android:elevation="10dp"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <ImageView
        android:id="@+id/fotoEstadisticaJ2"
        android:layout_width="25dp"
        android:layout_height="25dp"
        android:elevation="10dp"
        app:layout_constraintTop_toTopOf="@id/tipo"
        app:layout_constraintRight_toLeftOf="@id/jugador2Duelo"
        app:layout_constraintBottom_toBottomOf="@id/tipo"/>

</androidx.constraintlayout.widget.ConstraintLayout>